import { MutationTree } from 'vuex';
import { GeneralState } from '../types';

// Export mutations
// @ts-ignore (something is called mutations elsewhere)
export const mutations: MutationTree<GeneralState> = {

};
